#profile-name, #profile-join {
  font-size: smaller;
}
#profile-stats {
  margin: 1em 0;
  hr {
    margin: 0.25em 0;
  }
}
#recent-comments {
  min-height: 22em;
  background-color: var(--color-surface-sunken);
  border-radius: 0.5em;
}
