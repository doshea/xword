<%# Replaces: words/match.js.erb (jquery_ujs + jQuery $('#word-match-results').html(...)) %>
<%# Turbo Stream: replaces the word match results area with the new search results %>
<%= turbo_stream.update "word-match-results" do %>
  <%= render partial: 'matches' %>
<% end %>
