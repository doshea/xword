// NOTE: Intentionally kept as .js.erb (not migrated to Turbo Stream).
// Called via $.ajax({ dataType: 'script' }) from edit_funcs.js — Turbo does not intercept this.
if(edit_app.save_counter == <%= @save_counter %>){
  edit_app.log_save();
  edit_app.update_clock();
}