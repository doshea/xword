- if object.errors.any?
  .xw-alert.xw-alert--error{role: 'alert'}
    %p= "There are #{pluralize(object.errors.count, 'error')} on this form"
    %ul
      - object.errors.full_messages.each do |msg|
        %li= msg
