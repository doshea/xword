%td.cell{:'data-row' => cell.row, :'data-col' => cell.col, :'data-index' => cell.index, :'data-num' => cell.cell_num, :'data-id' => cell.id}
  .letter
    = cell.crossword.letters[index] if @solution
  .cell_num
    = cell.cell_num
  .cell_flag
