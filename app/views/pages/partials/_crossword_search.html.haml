%h2.search-result-count= "#{pluralize(@crosswords.length, 'crossword')} found"
- @crosswords_sliced.each do |slice|
  .xw-grid.xw-grid--auto
    - slice.each do |cw|
      = render partial: 'crosswords/partials/crossword_tab', locals: {cw: cw}