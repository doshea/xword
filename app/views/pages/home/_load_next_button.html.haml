= button_to((remaining.length <= Crossword.per_page) ? "Load last #{remaining.length}" : "Load next #{Crossword.per_page}", batch_crosswords_path(ids: remaining.map{|cw| cw.id}), remote: true, class: 'button secondary')