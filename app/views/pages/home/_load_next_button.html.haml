/ Replaced: button_to with remote: true → button_to without (Turbo intercepts form automatically)
= button_to((remaining.length <= Crossword.per_page) ? "Load last #{remaining.length}" : "Load next #{Crossword.per_page}", batch_crosswords_path(ids: remaining.map{|cw| cw.id}), class: 'button secondary', data: { disable_with: 'Loading...' })
-# TODO THIS is BROKEN. The request URI is too large for most users